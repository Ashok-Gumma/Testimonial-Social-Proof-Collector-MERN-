{
  "installCommand": "cd client && npm install",
  "buildCommand": "cd client && npm run build && node -e \"const fs = require('fs'); if (fs.existsSync('dist')) fs.cpSync('dist', '../dist', {recursive:true});\"",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
